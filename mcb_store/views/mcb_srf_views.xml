<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_mcb_srf_list" model="ir.ui.view">
        <field name="name">mcb.srf.list</field>
        <field name="model">mcb.srf</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="requester_id"/>
                <field name="project_id"/>
                <field name="date_request"/>
                <field name="source_location_id"/>
                <field name="dest_location_id"/>
                <field name="picking_id"/>
                <field name="state" widget="badge" decoration-success="state == 'issued'"/>
            </list>
        </field>
    </record>

    <record id="view_mcb_srf_form" model="ir.ui.view">
        <field name="name">mcb.srf.form</field>
        <field name="model">mcb.srf</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_request" type="object" string="Request" class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_approve" type="object" string="Approve" class="btn-primary"
                            invisible="state != 'requested'" groups="stock.group_stock_user"/>
                    <button name="action_issue" type="object" string="Issue (Create Transfer)"
                            class="btn-primary" invisible="state != 'approved'"
                            groups="stock.group_stock_user"/>
                    <button name="action_cancel" type="object" string="Cancel"
                            invisible="state in ('issued', 'cancelled')"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title"><h1><field name="name" readonly="1"/></h1></div>
                    <group>
                        <group>
                            <field name="requester_id"/>
                            <field name="project_id"/>
                            <field name="analytic_account_id"/>
                            <field name="recipient_name"/>
                        </group>
                        <group>
                            <field name="date_request"/>
                            <field name="source_location_id"/>
                            <field name="dest_location_id"/>
                            <field name="picking_id" readonly="1"/>
                        </group>
                    </group>
                    <field name="line_ids">
                        <list editable="bottom">
                            <field name="product_id"/>
                            <field name="quantity"/>
                            <field name="purpose"/>
                        </list>
                    </field>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_mcb_srf" model="ir.actions.act_window">
        <field name="name">Store Requisitions (SRF)</field>
        <field name="res_model">mcb.srf</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
